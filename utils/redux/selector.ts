"use client"

const isToggleSiderSelector = (state:any) => state.isToggleSider.value;
const statisticSelector = (state:any) => state.statistics.value;

export {isToggleSiderSelector, statisticSelector}